doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title Document
        link(rel="stylesheet", href="CSS\\style.css")
    body 
        nav
            //- img(src="Images\\logo.jpg", alt="Bad connection").nav-logo
            .nav-logo Company name
            div.nav-main
                button.nav-item(onclick="Set15()") Time 15s
                button.nav-item(onclick="Set30()") Time 30s
                button.nav-item(onclick="Set60()") TIme 60s
                span.nav-item About US
            main
                //- img(src="Images\\background.jpg", alt="").background
                div.article
                    //- h1 Beautiful Landing Page
                    #timer 30
                    br
                    .container
                        span.blank#blank
                        span.text#raw_text A graph is regular if the degree of every node is a constant d. A graph is complete if the degree of every node is n - 1, i.e., the graph contains all possible edges between the nodes.
                        textarea#myTextarea(name="a", cols="100", rows="10" autofocus) 
                        span. 
                    br
                    div#result 0 w/m
        script(src="script.js")